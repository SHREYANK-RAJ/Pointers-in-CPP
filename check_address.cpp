#include <iostream>
using namespace std;

int main() {
    int num = 5;

    // Prints the value of num
    cout << "num : " << num << endl;

    // Prints the address where num is stored in memory
    cout << "Address of num : " << &num << endl;

    return 0;
}
